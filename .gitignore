# ───────────────────────── Python ─────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/
env/
ENV/

# ─────────────────────── Editors / IDE ────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# ─────────────────────── Logs / runtime ───────────────────
*.log
*.pid
*.lock
nohup.out
streamlit_ema.log

# ──────────────────────── Secrets ─────────────────────────
# Real password lives in deploy/investeq.env on the VM; never commit.
deploy/investeq.env
*.env
.env
.env.*
*.pem
*.key
deploy/gcp_ssh_key.txt
credentials.json
auth_users.json
token.json
.totp_secret

# ────────────────── Large data directories ────────────────
# OHLC + options data — kept on the VM, never in source control.
DATA/
ema_scanner/data/
ema_scanner/data_*/
ema_scanner/scan_*.parquet
ema_scanner/*.parquet
daily_swing_scanner/data/

# Scanner per-date cache
**/scan_cache/
**/ltp.parquet
**/watchlist.txt

# ─────────────── Third-party bundled tooling ──────────────
# 250+ MB DLLs and 237 MB zip from the SciCode validator bundle.
turing/SciCode-Colab-Validator-Windows/
turing/SciCode-Colab-Validator-Windows.zip

# ML/DL experiment outputs (notebooks fine, large checkpoints not)
ML_DL/**/*.pt
ML_DL/**/*.pth
ML_DL/**/*.bin
ML_DL/**/*.h5
ML_DL/**/checkpoints/

# Large notebooks with embedded outputs
nifty_options_ml.ipynb

# ─────────────────────── Misc / temp ──────────────────────
*.bak
*.tmp
*.orig
*~

# ──────────── Generated analysis outputs ──────────────────
oi_options_trading/notebooks/oi_strategy_grid_results.csv
trail_variants_results.csv
.tmp/


# Expiry Blast — keep canonical output/, ignore parameter-sweep scratch dirs
expiry_blast/output_*/
expiry_blast/sweep/

# Resistance Marker — live-monitor accumulated daily setup lists (runtime output)
stock_resistance/found/
# Resistance Scanner — persisted 30-day scan snapshots (runtime cache)
stock_resistance/scan_cache/

# Scratch / dev-only scripts — kept locally, not part of the deployed server set.
# (_regen_straddle_pnl.py is the deprecated dup of regen_straddle_pnl.py and is
#  already excluded by deploy.sh.)
_regen_straddle_pnl.py
_data_freshness.py
_quick_scan.py
